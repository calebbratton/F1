DB_HOST=localhost
DB_PORT=5432
DB_USER=f1
DB_PASSWORD=f1password
DB_NAME=f1
SERVER_PORT=8080

# Uncomment to require X-API-Key header on all requests
# API_KEY=changeme

# Rate limiting (requests per second per IP, and burst size)
RATE_LIMIT_RPS=30
RATE_LIMIT_BURST=60

# Set to true during race weekends to connect to the F1 live timing stream
ENABLE_INGESTOR=false
